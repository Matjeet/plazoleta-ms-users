server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/users
    username: root
    password:
  application:
    name: "microservice-users"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
appDescription: "Pragma Powerup"
appVersion: "1.0.0"
